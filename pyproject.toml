[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sweetrpg-shared-web"
version = "0.13.2"
description = "Shared web frontend for SweetRPG"
readme = "README.md"
license = "MIT"
requires-python = ">=3.14"
authors = [
    { name = "Paul Schifferer", email = "dm@sweetrpg.com" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Web Environment",
    "Framework :: Flask",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Topic :: Internet :: WWW/HTTP :: WSGI :: Application",
]
dependencies = [
    "analytics-python~=1.0",
    "blinker~=1.0",
    "Flask-Caching~=2.0",
    "Flask-CORS~=5.0",
    "Flask-DotEnv~=0.1",
    "Flask-Session~=0.5",
    "Flask~=3.0",
    "hiredis~=3.0",
    "opentelemetry-exporter-otlp-proto-http~=1.44",
    "opentelemetry-instrumentation-flask~=0.65b0",
    "opentelemetry-sdk~=1.44",
    "prometheus-flask-exporter~=0.22",
    "python-dateutil~=2.0",
    "python-dotenv~=1.0",
    "python-editor~=1.0",
    "python-json-logger~=4.1",
    "python-logstash-async~=2.5",
    "PyYAML~=6.0",
    "redis~=5.0",
    "requests~=2.0",
    "sentry-sdk[flask]~=2.0",
    "sweetrpg-web-core",
    "flask-babel~=4.0",
    "sweetrpg-admin-api-client @ git+https://github.com/sweetrpg/admin-api-client.py.git@develop",
    "urllib3~=2.0",
    "uwsgi~=2.0",
]

[project.urls]
Documentation = "https://sweetrpg.github.io/sweetrpg/shared-web"

[project.scripts]
sweetrpg-shared-web = "sweetrpg_shared_web.cli:main"

[tool.hatch.metadata]
allow-direct-references = true

[dependency-groups]
test = [
    "asgiref",
    "blinker",
    "coverage-badge==1.1.0",
    "pytest",
    "pytest-cov==4.0.0",
    "python-dotenv~=1.0",
]
docs = [
    "Pallets-Sphinx-Themes",
    "Sphinx",
    "sphinx-issues",
    "sphinxcontrib-log-cabinet",
    "sphinx-tabs",
    "rstcheck",
]

[tool.hatch.build.targets.wheel]
packages = ["src/sweetrpg_shared_web"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error"]

[tool.coverage.run]
branch = true
source = [
    "sweetrpg_shared_web",
    "tests",
]

[tool.coverage.paths]
source = [
    "src",
    "*/site-packages",
]
