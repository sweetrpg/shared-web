---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor

metadata:
    name: sweetrpg-shared-web
    labels:
        release: prometheus

spec:
    namespaceSelector:
        matchNames:
            - sweetrpg-shared
    podMetricsEndpoints:
        - port: http
    selector:
        matchLabels:
            app.kubernetes.io/name: shared-web
            app.kubernetes.io/version: v1
            app.kubernetes.io/component: web
            app.kubernetes.io/part-of: shared
