---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sweetrpg-support

resources:
    - namespace.yaml
    - ../../base
    - configmaps.yaml
patchesStrategicMerge:
    - replicas.yaml

commonLabels:
    platform: sweetrpg
    app: sweetrpg-shared-web-v1
    component: web
    package: shared
    version: v1

generatorOptions:
    disableNameSuffixHash: true
