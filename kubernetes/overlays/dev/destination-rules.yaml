---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule

metadata:
    name: shared-web

spec:
    host: web-v1.sweetrpg-shared.svc.cluster.local
    trafficPolicy:
        loadBalancer:
            simple: LEAST_CONN
