---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler

metadata:
    name: shared-web-v1

spec:
    scaleTargetRef:
        apiVersion: apps/v1
        kind: Deployment
        name: shared-web-v1
    minReplicas: 1
    maxReplicas: 10
    metrics:
        - type: Resource
          resource:
              name: cpu
              target:
                  type: Utilization
                  averageUtilization: 50
