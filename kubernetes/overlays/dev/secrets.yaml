---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: shared-web-auth

spec:
    refreshInterval: "1h"
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: shared-web-auth
        creationPolicy: Owner
    dataFrom:
        - extract:
              key: Auth0

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: shared-web-misc

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: shared-web-misc
        creationPolicy: Owner
    dataFrom:
        - extract:
              key: /sweetrpg/support/assets/web/misc

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: shared-web-cache

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: shared-web-cache
        creationPolicy: Owner
    dataFrom:
        - extract:
              key: /sweetrpg/support/cache/client

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: shared-web-files

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: shared-web-files
        creationPolicy: Owner
        template:
            type: Opaque
            data:
                newrelic.ini: "{{ .contents | toString }}"
    data:
        - secretKey: contents
          remoteRef:
              key: NewRelic
