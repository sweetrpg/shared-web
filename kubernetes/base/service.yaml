---
kind: Service
apiVersion: v1

metadata:
    name: web

spec:
    selector:
        app: shared
        component: web
        platform: sweetrpg
    type: LoadBalancer
    ports:
        - name: http
          port: 8080
          targetPort: http
