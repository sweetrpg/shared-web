---
kind: Service
apiVersion: v1

metadata:
    name: web-v1
    annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"

spec:
    selector:
        platform: sweetrpg
        app: web
        component: web
        package: shared
        version: v1
    type: LoadBalancer
    ports:
        - name: http
          port: 8081
          targetPort: http
